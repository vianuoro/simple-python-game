# simple-python-game
Single Python file using Pygbag that runs a simple Pygame game in a web browser. The game should be as minimal as possible — just a colored square the player can move with arrow keys. Include all setup instructions to get it running locally and in the browser.

"""
Simple Pygame + Pygbag game — move a square with arrow keys.

=== SETUP ===

1. Install dependencies:
       pip install pygame pygbag

2. Run locally (standard pygame window):
       python main.py

3. Run in the browser:
       pygbag main.py
   Then open http://localhost:8000 in your browser.

NOTE: pygbag requires the file to be named main.py and the
      async/await pattern shown below — that's all it needs.
"""

